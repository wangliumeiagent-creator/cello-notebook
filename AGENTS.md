# 项目协作约定

先阅读 [交接说明](docs/HANDOFF.md) 和 [开发与发布](docs/DEVELOPMENT.md)。

- 此目录才是项目 Git 根目录；不要在父目录的仓库提交本项目。
- 修改前检查 `git status` 和 `git diff`；保护用户已有改动和未跟踪文件。
- 不删除文件，不用 reset --hard、clean、强制推送或覆盖操作解决同步冲突。
- 编辑 `src/`；`dist/index.html` 和 MusicXML 由 `npm run build` 生成，PDF 另行更新。
- 用户要求的按钮文字是“显示辅助”；不恢复 MusicXML 下载按钮或已移除的 iPhone 无声提示文字。
- 保留 iPhone 音频初始化中的 `navigator.audioSession.type='playback'` 兼容处理。
- 保留低音谱号、音名/弦名/指法分行、可隐藏简谱、20/30 BPM 和练习记录导入导出。
- 原曲使用原拍号；四小节限制只适用于原创音阶。不要把拓展曲目宣称为全程第一把位。
- 两个天空之城版本分别维护；移调、改八度、伸张或换把必须明确说明。
- 新增谱面需逐小节校验拍数；区分连弓与延音，验证三连音和一二房播放路线。
- 指法是编辑参考；原图未给出的指法不能宣称来自原谱。
- 教材 PDF、原始谱图、个人练习记录和凭据不提交到公开仓库。
- 代码改动运行 `npm test`、构建；谱面改动另检查 XML、桌面/手机布局、音频与打印。
- 浏览器检查使用 `PLAYWRIGHT_PATH`、`BROWSER_PATH`，不要复制旧电脑的绝对路径到新代码。
- main 保存源码，gh-pages 只发布 dist。发布步骤与失败处理见开发文档。
- 提交前检查生成产物和文档一致；不要将“构建通过”当作 PDF 已更新或网站已部署。
